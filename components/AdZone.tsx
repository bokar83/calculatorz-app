// Auto Ads is enabled via the AdSense script in layout.tsx.
// Google places ad units automatically — manual ins tags are not used.
export default function AdZone({ zone: _zone, className: _className }: { zone: number; className?: string }) {
  return null
}
